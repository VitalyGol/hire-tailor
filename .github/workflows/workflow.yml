name: CI workflow
jobs:
    build-server:
      runs-on: ubuntu-latest
      container: python:3.12

      defaults:
        run:
          working-directory: server
      
      steps:
        - name: actions/checkout@v3
          uses: actions/checkout@v3

        - name: Install dependencies
          run: |
              python -m pip install --upgrade pip
              pip install -r requirements.txt
        
        - name: Lint code
          run: pylint service
    
    build-client:
      runs-on: ubuntu-latest
      container: node:20-alpine

      defaults:
        run:
          working-directory: web/hire-tailor

      steps:
        - name: actions/checkout@v3
          uses: actions/checkout@v3

        - name: Install dependencies
          run: npm ci
        
        - name: Lint code
          run: npm run lint


on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
